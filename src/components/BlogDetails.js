import React from 'react'

const BlogDetails = ({ blogs }) => {
    return (
        <div>
            <div>BlogDetails</div>
            <p>{blogs.title}</p>
        </div>
    )
}

export default BlogDetails